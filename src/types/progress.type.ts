
export interface ProgressType {
    icon: string; // Icon name or path
    name: string; // Skill name
    percentage: number; // Proficiency percentage
    color?: string; // Color class for the progress bar
};